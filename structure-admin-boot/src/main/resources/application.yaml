server:
  port: 8080
spring:
  profiles:
    active: dev
  application:
    name: basic-admin
  main:
    allow-circular-references: true
management:
  endpoint:
    health:
      show-details: always
  health:
    mail:
      enabled: false
#  server:
#    port: 7777