server:
  port: 18000
spring:
  profiles:
    active: dev
  application:
    name: basic-admin
  main:
    allow-circular-references: true
#    allow-bean-definition-overriding: true
management:
  endpoint:
    health:
      show-details: always